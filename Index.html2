<!DOCTYPE html>
<html lang="pt-br">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>R-NET - Nome</title>

<style>
/* Fundo */
body {
    margin: 0;
    background-color: #f2f2f2;
    font-family: Arial, Helvetica, sans-serif;

    display: flex;
    flex-direction: column;
    align-items: center;
    height: 100vh;
}

/* Logo no topo */
.logo {
    width: 200px;
    margin-top: 50px;
}

/* Área central */
.container {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
}

/* Texto principal */
.titulo {
    font-size: 40px;
    text-align: center;
    color: #ffffff;
    -webkit-text-stroke: 2px #ff2f7a; /* contorno rosa */
    text-shadow: 0 4px 10px rgba(255, 47, 122, 0.3);
    font-weight: bold;
    margin-bottom: 40px;
}

/* Caixa de nome */
.nome-box {
    width: 420px;
    max-width: 90%;
}

/* Campo de digitar */
.nome-box input {
    width: 100%;
    padding: 18px;
    border-radius: 40px;
    border: 1px solid #ccc;
    font-size: 18px;
    text-align: center;
    outline: none;
    box-shadow: 0 4px 10px rgba(0,0,0,0.1);
}

/* Efeito ao clicar */
.nome-box input:focus {
    border: 2px solid #ff2f7a;
    box-shadow: 0 0 12px rgba(255, 47, 122, 0.4);
}
</style>
</head>

<body>

<!-- Logo -->
<img src="logo.png" class="logo" alt="R-NET">

<!-- Conteúdo central -->
<div class="container">

    <div class="titulo">
        qual seu nome?<br>
        (no rpop)
    </div>

    <div class="nome-box">
        <input type="text" placeholder="Digite seu nome no RPOP">
    </div>

</div>

</body>
</html>
